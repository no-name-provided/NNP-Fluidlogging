pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
        maven { url = 'https://maven.neoforged.net/releases' }
    }
}

plugins {
    // Version required bump for Gradle version 9? Resolves java versions, supposedly, to fix "id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"" error
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}
